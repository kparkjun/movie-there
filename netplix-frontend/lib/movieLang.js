export const getMovieTitle = (m) => m?.movieName;

export const getPosterPath = (m) => m?.posterPath;

export const getBackdropPath = (m) => m?.backdropPath;

export const getOverview = (m) => m?.overview;

export const getTagline = (m) => m?.tagline;
